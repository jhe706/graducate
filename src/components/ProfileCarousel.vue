<template>
	
</template>

<script>
export default {
	// props: ['getMatchMap'],
	// methods: {
	// 	getMatches(){
	// 		let map = this.getMatchMap();

	// 		let myMatches = matchMap.get(uuid) ? matchMap.get(uuid) : []; // should return list of match uuids
	// 		matchesRef.child(uuid).set(myMatches);
			
	// 		console.log("User's matches: ", myMatches);
	// 		return myMatches;
	// 	}
	// }
}
</script>
